<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- 9:16 फुल स्क्रीन वीडियो -->
    <WebView
        android:id="@+id/reelWebView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- नीचे क्रिएटर की जानकारी और कैप्शन (Instagram Style) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@id/sideActionButtons"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingBottom="24dp">

        <TextView
            android:id="@+id/tvReelCreator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:shadowColor="#000000"
            android:shadowDx="1"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:text="@desi_reels"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvReelCaption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:shadowColor="#000000"
            android:shadowDx="1"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:text="वायरल रील धमाका 🔥"
            android:textColor="#EEEEEE"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/tvReelMusic"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="🎵 Original Sound"
            android:textColor="#DDDDDD"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- दाएँ हाथ पर Instagram जैसे एक्शन बटन्स -->
    <LinearLayout
        android:id="@+id/sideActionButtons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="24dp"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <!-- Like -->
        <TextView
            android:id="@+id/btnLike"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="6dp"
            android:text="🤍"
            android:textSize="26sp" />

        <!-- Comment -->
        <TextView
            android:id="@+id/btnComment"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:padding="6dp"
            android:text="💬"
            android:textSize="24sp" />

        <!-- Share -->
        <TextView
            android:id="@+id/btnShare"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:padding="6dp"
            android:text="✈️"
            android:textSize="24sp" />

        <!-- Download -->
        <TextView
            android:id="@+id/btnDownload"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:padding="6dp"
            android:text="⬇️"
            android:textSize="22sp" />
    </LinearLayout>

    <!-- चकरी (छिपी हुई) -->
    <ProgressBar
        android:id="@+id/reelLoader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

</RelativeLayout>
